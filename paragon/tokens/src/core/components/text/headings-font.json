{
  "$description": "Headings use the same brand font as body (inherit). Change typeface only in core/global/typography.json. Template B (Harvard-PLL / tels-mirror): headings are serif at normal weight, not bold — tels-mirror's own `h1,h2,h3,h4 { font-weight: 400 }`.",
  "typography": {
    "headings": {
      "font": {
        "family": {
          "$value": "{typography.font.family.base}",
          "$type": "fontFamily",
          "$description": "Heading font → same as body (--pgn-typography-font-family-base)"
        },
        "weight": {
          "$value": "400",
          "$type": "fontWeight",
          "$description": "Heading font weight — normal, not bold, per tels-mirror"
        }
      },
      "line-height": {
        "$value": "1.25",
        "$type": "number",
        "$description": "Headings line-height → --pgn-typography-headings-line-height"
      }
    }
  }
}
